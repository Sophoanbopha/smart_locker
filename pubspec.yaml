name: smart_locker_mqtt
description: IoT Smart Locker System — Flutter + MQTT + ESP32

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.4.0
  firebase_auth: ^5.3.0
  firebase_database: ^11.0.0
  # Optional: local notifications for locker alerts
  # flutter_local_notifications: ^17.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
